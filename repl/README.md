create a repl in nodejs is really easy,

we can expose the variable by attach property to context in replServer,
these variable can be used in repl


```javascript
    node replDemo.js

    moment.month(10)

```
